# Damage detection (Track B). Without this the pipeline still runs geometry;
# damage frames report an error and the scope comes out empty.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: SAM 2 mask refinement. (NOT TESTED)
